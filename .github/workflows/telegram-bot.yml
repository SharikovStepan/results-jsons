name: Telegram JSON File Processor

on:
  repository_dispatch:
    types: [telegram_file]

jobs:
  process_file:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - uses: appleboy/telegram-action@master
        id: telegram
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          message: "Отправьте JSON-файл с данными. В подписи укажите название."

      - name: Process JSON file
        if: steps.telegram.outputs.document
        run: |
          # Устанавливаем инструменты
          sudo apt-get -y install jq

          # Получаем данные
          FILENAME_PREFIX="${{ steps.telegram.outputs.caption || 'file'}}"
          JSON_DATA='${{ steps.telegram.outputs.document }}'
          ORIG_FILENAME="${{ steps.telegram.outputs.document_file_name }}"

          # 1. Пробуем извлечь дату из структуры JSON
          DATETIME_RAW=$(echo "$JSON_DATA" | jq -r '
            [.heats | to_entries[] | 
              select(.value.rounds != null) | 
              .key as $heat | 
              .value.rounds | to_entries[] | 
              select(.value.start_time_formatted != null) | 
              .value.start_time_formatted
            ] | first // "null"')

          # 2. Если в JSON нет даты - пробуем извлечь из имени файла
          if [ "$DATETIME_RAW" = "null" ]; then
            if [[ "$ORIG_FILENAME" =~ RotorHazard_Export_([0-9]{4})([0-9]{2})([0-9]{2})_([0-9]{2})([0-9]{2})([0-9]{2}) ]]; then
              DATETIME_RAW="${BASH_REMATCH[1]}-${BASH_REMATCH[2]}-${BASH_REMATCH[3]} ${BASH_REMATCH[4]}:${BASH_REMATCH[5]}:${BASH_REMATCH[6]}"
            fi
          fi

          # 3. Форматируем дату (стандартизируем)
          if [ "$DATETIME_RAW" != "null" ]; then
            DATETIME=$(echo "$DATETIME_RAW" | sed 's/ /_/g; s/\.[0-9]*$//; s/:/-/g')
          else
            DATETIME=$(date +"%Y-%m-%d_%H-%M-%S")
          fi

          # Создаем имя файла
          FILENAME="${FILENAME_PREFIX}_${DATETIME}.json"
          
          # Сохраняем файл
          mkdir -p results.jsons
          echo "$JSON_DATA" > "results.jsons/$FILENAME"

          # Коммитим
          git config --global user.name "Telegram Bot"
          git config --global user.email "bot@github.com"
          git add .
          git commit -m "Добавлен файл: $FILENAME"
          git push

      - uses: appleboy/telegram-action@master
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          message: "Файл сохранён как: $FILENAME"
